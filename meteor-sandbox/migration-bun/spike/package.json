{
  "name": "spike",
  "version": "1.0.0",
  "description": "Proof of concept: a standard Meteor server bundle (`meteor build --directory`) boots via ESM `import()` instead of the legacy `boot.js` + `runtime.js` + `vm.runInThisContext` chain.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.20.0"
  }
}
